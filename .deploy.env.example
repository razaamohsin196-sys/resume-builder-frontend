# Copy to .deploy.env and set your AWS Lightsail instance details.
# Do not commit .deploy.env (it is gitignored).

# Lightsail instance public IP (e.g. from Lightsail console)
SERVER_IP=YOUR_LIGHTSAIL_IP

# Usually "ubuntu" for Ubuntu Node.js / Linux instances
SERVER_USER=ubuntu
